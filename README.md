## Build Setup

```bash
# install composer
$ composer install

# copy and paste .env.example and rename it to .env
# in the .env file , change the DB_DATABASE to DB_DATABASE=emotions

# run php artisan 
$ php artisan key:generate
$ php artisan migrate

# start running your XAMPP

# run it on localhost
$ php artisan serve
```
